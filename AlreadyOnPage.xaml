<Window x:Class="EAccess.Client.AlreadyOnPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EAccess.Client"
        Title="Уведомление"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2A2E3A"/>
        <SolidColorBrush x:Key="OverlayBrush" Color="#B3000000"/>
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource PrimaryColor1}" Offset="0"/>
            <GradientStop Color="{StaticResource PrimaryColor2}" Offset="1"/>
        </LinearGradientBrush>
        

        <Style x:Key="OkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="18">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.92"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource OverlayBrush}">
        <Border Background="{StaticResource PrimaryBrush}" CornerRadius="24" Padding="32" SnapsToDevicePixels="True" Width="777" Height="494" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,10,0,30" HorizontalAlignment="Center">
                    <Image Source="pack://application:,,,/EAccess.Client;component/pictures/logo.png"
                       Width="175" Height="153" Margin="0,0,0,12"/>
                    <TextBlock Text="Страж" FontFamily="{StaticResource AppFont}" FontSize="48" FontWeight="Bold" Foreground="{StaticResource WhiteBrush}" HorizontalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Row="1"
                       Text="Вы уже на данной страннице!"
                           FontFamily="{StaticResource AppFont}"
                           FontSize="36"
                           FontWeight="Bold"
                           Foreground="{StaticResource WhiteBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>

                <Border Grid.Row="2" HorizontalAlignment="Right" Margin="0,30,8,0" CornerRadius="18" Background="{StaticResource PrimaryGradient}" Width="250" Height="70" VerticalAlignment="Bottom">
                    <Button x:Name="OkButton"
                            Click="OkButton_Click"
                            Style="{StaticResource OkButtonStyle}"
                            Content="Хорошо"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>