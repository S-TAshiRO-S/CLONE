<Window x:Class="EAccess.Client.NoEventPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EAccess.Client"
        Title="Уведомление"
        Width="777" Height="494"
        MinWidth="777" MinHeight="494"
        MaxWidth="777" MaxHeight="494"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <Color x:Key="PrimaryColor1">#F54B64</Color>
        <Color x:Key="PrimaryColor2">#F78361</Color>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2A2E3A"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="#FFFFFF"/>
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource PrimaryColor1}" Offset="0"/>
            <GradientStop Color="{StaticResource PrimaryColor2}" Offset="1"/>
        </LinearGradientBrush>
        <FontFamily x:Key="AppFont">pack://application:,,,/EAccess.Client;component/fonts/#Ali-Bold</FontFamily>
    </Window.Resources>

    <Border Background="{StaticResource PrimaryBrush}" CornerRadius="24" Padding="32" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,10,0,30" HorizontalAlignment="Center">
                <Viewbox Width="116" Height="116" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <Grid Width="116" Height="116">
                        <Grid.Resources>
                            <LinearGradientBrush x:Key="ShieldGradient" StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFA5B6B" Offset="0"/>
                                <GradientStop Color="#FFF97063" Offset="1"/>
                            </LinearGradientBrush>
                        </Grid.Resources>
                        <Path Fill="{StaticResource ShieldGradient}" Stretch="Fill" Data="M58,8 L100,24 L100,62 C100,85 79,104 58,108 C37,104 16,85 16,62 L16,24 Z"/>
                        <Path Fill="#FFFFFF" Stretch="Fill" Data="M72.5,46.5 L66.5,52.5 L55,66 L45.5,56.5 L39.5,62.5 L55,78 L78.5,50.5 Z" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Viewbox>
                <TextBlock Text="Страж" FontFamily="{StaticResource AppFont}" FontSize="48" Foreground="{StaticResource WhiteBrush}" HorizontalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>

            <TextBlock Grid.Row="1"
                       Text="Вы еще не создали мероприятие!"
                       FontFamily="{StaticResource AppFont}"
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="{StaticResource WhiteBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>

            <Border Grid.Row="2" HorizontalAlignment="Right" Margin="0,30,8,0" CornerRadius="18" Background="{StaticResource PrimaryGradient}" Width="250" Height="70" VerticalAlignment="Bottom">
                <Button x:Name="OkButton"
                        Click="OkButton_Click"
                        Background="Transparent"
                        BorderThickness="0"
                        FontFamily="{StaticResource AppFont}"
                        FontSize="32"
                        Foreground="{StaticResource WhiteBrush}"
                        Content="Хорошо"
                        Cursor="Hand" FontWeight="Bold"/>
            </Border>
        </Grid>
    </Border>
</Window>